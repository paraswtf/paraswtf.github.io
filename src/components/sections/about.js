import React, { useEffect, useRef } from 'react';
import { StaticImage } from 'gatsby-plugin-image';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';
import Tilt from 'react-parallax-tilt';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(140px, 200px));
    grid-gap: 0 10px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--green);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;
const StyledPic = styled.div`
  position: relative;
  max-width: 300px;

  @media (max-width: 768px) {
    margin: 50px auto 0;
    width: 70%;
  }

  .wrapper {
    ${({ theme }) => theme.mixins.boxShadow};
    display: block;
    position: relative;
    width: 100%;
    border-radius: var(--border-radius);
    background-color: var(--green);

    &:hover,
    &:focus {
      cursor: pointer;
      outline: 0;

      &:after {
        top: 15px;
        left: 15px;
      }

      .img {
        filter: none;
        mix-blend-mode: normal;
      }
    }

    .img {
      position: relative;
      border-radius: var(--border-radius);
      /*mix-blend-mode: multiply;*/
      /*filter: grayscale(100%) contrast(1);*/
      transition: var(--transition);
    }

    &:before,
    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: var(--border-radius);
      transition: var(--transition);
    }

    &:before {
      top: 0;
      left: 0;
      background-color: var(--navy);
      mix-blend-mode: screen;
    }

    &:after {
      border: 2px solid var(--green);
      top: 20px;
      left: 20px;
      z-index: -1;
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = ['JavaScript (ES6+)', 'TypeScript', 'React', 'NextJS', 'Node.js', 'WordPress'];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="numbered-heading">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
              Hello! My name is Paras and I like creating cool things to solve unique problems. My
              interest in web development started back in 2018 when I decided to create{' '}
              <a href="https://github.com/paraswtf/Ririchiyo">Ririchiyo</a>, an integration for
              Discord that let users play music and store playlists — like any sane person, I used
              to bother every developer I found on online communities with the stupidest problems
              while working on it, but I guess that's how I learnt!
            </p>
            <p>
              During the pandemic in 2020, we had a lot of students from our school on discord
              studying together, they helped me a lot in testing and refining the music integration
              on discord.
            </p>

            <p>
              Fast-forward to today, and I’ve had the privilege of working for{' '}
              <a href="https://tunebot.org/">tunebot.org</a>,{' '}
              <a href="https://www.instagram.com/challengefitness007/">Challenge Fitness</a>, and{' '}
              <a href="https://www.instagram.com/am._designs/">AM Designs</a>, a small art business.
            </p>

            <p>
              I have also recently been working on a{' '}
              <a href="https://s.styxo.codes/">URL shortner</a> that lets people shorten their
              links, create custom links, create profile pages with links and get analytics data.
              It's still a work in progress, but you can still check it out :)
            </p>

            <p>Here are a few technologies I’ve been working with recently:</p>
          </div>

          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
        </StyledText>

        <Tilt tiltMaxAngleX={2} tiltMaxAngleY={2}>
          <StyledPic>
            <div className="wrapper">
              <StaticImage
                className="img"
                src="../../images/me.jpg"
                width={500}
                quality={95}
                formats={['AUTO', 'WEBP', 'AVIF']}
                alt="Headshot"
              />
            </div>
          </StyledPic>
        </Tilt>
      </div>
    </StyledAboutSection>
  );
};

export default About;
